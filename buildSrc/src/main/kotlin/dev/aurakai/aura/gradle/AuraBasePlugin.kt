package dev.aurakai.aura.gradle

import org.gradle.api.Plugin
import org.gradle.api.Project

class AuraBasePlugin : Plugin<Project> {
    override fun apply(target: Project) {
        // Base plugin logic for Aura projects
        // You can add shared configuration here
    }
}

